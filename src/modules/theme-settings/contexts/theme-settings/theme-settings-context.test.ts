import { describe, expect, it } from "vitest";

import { ThemeSettingsContext } from "./theme-settings-context";

describe("ThemeSettingsContext", () => {
  it("is created with null as default value", () => {
    expect(ThemeSettingsContext).toBeDefined();
  });
});
