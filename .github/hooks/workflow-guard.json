{
  "description": "Workflow guard hook - validates workflow state before tool execution",
  "version": 1,
  "hooks": {
    "preToolUse": [
      {
        "type": "command",
        "command": "node --experimental-strip-types --no-warnings bin/vibe task workflow:hook -- workflow-guard",
        "cwd": ".",
        "timeout": 10000,
        "description": "Validates that the current workflow phase allows the requested action"
      }
    ]
  },
  "notes": "This hook uses the same script as Claude Code to maintain consistency"
}
